module.exports = {
    presets: [
        '@babel/preset-env',
        '@babel/preset-react'
    ], // Conjuntos de configurações que podem ser reaproveitadas
    plugins: [
        '@babel/plugin-transform-runtime'
    ]
};